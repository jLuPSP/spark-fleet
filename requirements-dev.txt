# Local tooling (cross-platform). Ansible lives in requirements-ci.txt (Linux/WSL only).
jsonschema>=4.21,<5
msal[broker]>=1.33,<2
msal-extensions[portalocker]>=1.3,<2
PyJWT[crypto]>=2.8,<3
PyYAML>=6.0,<7
pytest>=8.0,<9
