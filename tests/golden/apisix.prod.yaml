# Generated by gateway/generate_apisix.py. DO NOT EDIT.
# mode: prod
# plan_hash: bd29f45b2febd5e60657ee095ba7b174524d871299ec7f67da758718cc50cb77
# APISIX 3.17.0 standalone rules file; the trailing #END marker is
# mandatory or APISIX silently ignores the whole file.
routes:
- id: sf-embedding-bge-m3
  name: sf-embedding-bge-m3
  uri: /v1/embeddings
  methods:
  - POST
  priority: 10
  vars:
  - - post_arg.model
    - ==
    - bge-m3
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local ui_b64 = core.request.header(ctx, "X-Userinfo")
          if not ui_b64 then
            return 401, { error = "no validated token context" }
          end
          local ui_json = ngx.decode_base64(ui_b64)
          local claims = ui_json and core.json.decode(ui_json) or nil
          if not claims then
            return 401, { error = "cannot decode userinfo" }
          end
          local have = {}
          if type(claims.roles) == "table" then
            for _, r in ipairs(claims.roles) do
              have[r] = true
            end
          end
          local team
          if not team and have["Fleet.TeamAlpha"] then team = "team-alpha" end
          if not team and have["Fleet.TeamQA"] then team = "team-qa" end
          if not team then
            return 403, { error = "token carries no fleet role" }
          end
          local allowed = {
            ["team-alpha"] = true,
          }
          if not allowed[team] then
            return 403, { error = "team " .. team .. " may not call model bge-m3" }
          end
          local quota = {
            ["team-alpha"] = "2000000",
          }
          core.request.set_header(ctx, "X-Fleet-Team", team)
          core.request.set_header(ctx, "X-Fleet-Token-Quota", quota[team])
        end
    workflow:
      rules:
      - case:
        - - http_x_fleet_team
          - ==
          - team-alpha
        actions:
        - - limit-count
          - count: 120
            time_window: 60
            key_type: constant
            key: bge-m3:team-alpha
            rejected_code: 429
            rejected_msg: '{"error": "request quota exceeded for team-alpha on bge-m3"}'
            policy: local
      - actions:
        - - return
          - code: 403
    ai-rate-limiting:
      limit_strategy: total_tokens
      rejected_code: 429
      rejected_msg: token budget exhausted for bge-m3
      show_limit_quota_header: true
      rules:
      - count: ${http_x_fleet_token_quota ?? 1000}
        time_window: 3600
        key: $http_x_fleet_team
        header_prefix: Team
    ai-proxy-multi:
      fallback_strategy:
      - http_429
      - http_5xx
      max_retries: 2
      retry_on_failure_within_ms: 2000
      timeout: 120000
      keepalive: true
      ssl_verify: false
      balancer:
        algorithm: roundrobin
      instances:
      - name: bge-m3-node2-p8000
        provider: openai-compatible
        weight: 1
        priority: 0
        auth:
          header:
            Authorization: Bearer fleet-internal-unused
        options:
          model: bge-m3
        override:
          endpoint: http://sf-bge-m3-node2-r0.spark-fleet.svc.cluster.local:8000/v1/embeddings
        checks:
          active:
            type: http
            http_path: /health
            timeout: 2
            healthy:
              interval: 2
              successes: 2
            unhealthy:
              interval: 1
              http_failures: 3
              timeouts: 3
              http_statuses:
              - 429
              - 500
              - 502
              - 503
              - 504
    prometheus:
      prefer_name: true
- id: sf-chat-qwen3-14b-awq
  name: sf-chat-qwen3-14b-awq
  uri: /v1/chat/completions
  methods:
  - POST
  priority: 10
  vars:
  - - post_arg.model
    - ==
    - qwen3-14b-awq
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local ui_b64 = core.request.header(ctx, "X-Userinfo")
          if not ui_b64 then
            return 401, { error = "no validated token context" }
          end
          local ui_json = ngx.decode_base64(ui_b64)
          local claims = ui_json and core.json.decode(ui_json) or nil
          if not claims then
            return 401, { error = "cannot decode userinfo" }
          end
          local have = {}
          if type(claims.roles) == "table" then
            for _, r in ipairs(claims.roles) do
              have[r] = true
            end
          end
          local team
          if not team and have["Fleet.TeamAlpha"] then team = "team-alpha" end
          if not team and have["Fleet.TeamQA"] then team = "team-qa" end
          if not team then
            return 403, { error = "token carries no fleet role" }
          end
          local allowed = {
            ["team-alpha"] = true,
            ["team-qa"] = true,
          }
          if not allowed[team] then
            return 403, { error = "team " .. team .. " may not call model qwen3-14b-awq" }
          end
          local quota = {
            ["team-alpha"] = "2000000",
            ["team-qa"] = "50000",
          }
          core.request.set_header(ctx, "X-Fleet-Team", team)
          core.request.set_header(ctx, "X-Fleet-Token-Quota", quota[team])
        end
    workflow:
      rules:
      - case:
        - - http_x_fleet_team
          - ==
          - team-alpha
        actions:
        - - limit-count
          - count: 120
            time_window: 60
            key_type: constant
            key: qwen3-14b-awq:team-alpha
            rejected_code: 429
            rejected_msg: '{"error": "request quota exceeded for team-alpha on qwen3-14b-awq"}'
            policy: local
      - case:
        - - http_x_fleet_team
          - ==
          - team-qa
        actions:
        - - limit-count
          - count: 5
            time_window: 60
            key_type: constant
            key: qwen3-14b-awq:team-qa
            rejected_code: 429
            rejected_msg: '{"error": "request quota exceeded for team-qa on qwen3-14b-awq"}'
            policy: local
      - actions:
        - - return
          - code: 403
    ai-rate-limiting:
      limit_strategy: total_tokens
      rejected_code: 429
      rejected_msg: token budget exhausted for qwen3-14b-awq
      show_limit_quota_header: true
      rules:
      - count: ${http_x_fleet_token_quota ?? 1000}
        time_window: 3600
        key: $http_x_fleet_team
        header_prefix: Team
    ai-proxy-multi:
      fallback_strategy:
      - http_429
      - http_5xx
      max_retries: 2
      retry_on_failure_within_ms: 2000
      timeout: 120000
      keepalive: true
      ssl_verify: false
      balancer:
        algorithm: roundrobin
      instances:
      - name: qwen3-14b-awq-node1-p8000
        provider: openai-compatible
        weight: 1
        priority: 0
        auth:
          header:
            Authorization: Bearer fleet-internal-unused
        options:
          model: qwen3-14b-awq
        override:
          endpoint: http://sf-qwen3-14b-awq-node1-r0.spark-fleet.svc.cluster.local:8000/v1/chat/completions
        checks:
          active:
            type: http
            http_path: /health
            timeout: 2
            healthy:
              interval: 2
              successes: 2
            unhealthy:
              interval: 1
              http_failures: 3
              timeouts: 3
              http_statuses:
              - 429
              - 500
              - 502
              - 503
              - 504
      - name: qwen3-14b-awq-node3-p8000
        provider: openai-compatible
        weight: 1
        priority: 0
        auth:
          header:
            Authorization: Bearer fleet-internal-unused
        options:
          model: qwen3-14b-awq
        override:
          endpoint: http://sf-qwen3-14b-awq-node3-r0-serve-svc.spark-fleet.svc.cluster.local:8000/v1/chat/completions
        checks:
          active:
            type: http
            http_path: /health
            timeout: 2
            healthy:
              interval: 2
              successes: 2
            unhealthy:
              interval: 1
              http_failures: 3
              timeouts: 3
              http_statuses:
              - 429
              - 500
              - 502
              - 503
              - 504
    prometheus:
      prefer_name: true
- id: sf-completion-qwen3-14b-awq
  name: sf-completion-qwen3-14b-awq
  uri: /v1/completions
  methods:
  - POST
  priority: 10
  vars:
  - - post_arg.model
    - ==
    - qwen3-14b-awq
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local ui_b64 = core.request.header(ctx, "X-Userinfo")
          if not ui_b64 then
            return 401, { error = "no validated token context" }
          end
          local ui_json = ngx.decode_base64(ui_b64)
          local claims = ui_json and core.json.decode(ui_json) or nil
          if not claims then
            return 401, { error = "cannot decode userinfo" }
          end
          local have = {}
          if type(claims.roles) == "table" then
            for _, r in ipairs(claims.roles) do
              have[r] = true
            end
          end
          local team
          if not team and have["Fleet.TeamAlpha"] then team = "team-alpha" end
          if not team and have["Fleet.TeamQA"] then team = "team-qa" end
          if not team then
            return 403, { error = "token carries no fleet role" }
          end
          local allowed = {
            ["team-alpha"] = true,
            ["team-qa"] = true,
          }
          if not allowed[team] then
            return 403, { error = "team " .. team .. " may not call model qwen3-14b-awq" }
          end
          local quota = {
            ["team-alpha"] = "2000000",
            ["team-qa"] = "50000",
          }
          core.request.set_header(ctx, "X-Fleet-Team", team)
          core.request.set_header(ctx, "X-Fleet-Token-Quota", quota[team])
        end
    workflow:
      rules:
      - case:
        - - http_x_fleet_team
          - ==
          - team-alpha
        actions:
        - - limit-count
          - count: 120
            time_window: 60
            key_type: constant
            key: qwen3-14b-awq:team-alpha
            rejected_code: 429
            rejected_msg: '{"error": "request quota exceeded for team-alpha on qwen3-14b-awq"}'
            policy: local
      - case:
        - - http_x_fleet_team
          - ==
          - team-qa
        actions:
        - - limit-count
          - count: 5
            time_window: 60
            key_type: constant
            key: qwen3-14b-awq:team-qa
            rejected_code: 429
            rejected_msg: '{"error": "request quota exceeded for team-qa on qwen3-14b-awq"}'
            policy: local
      - actions:
        - - return
          - code: 403
    ai-rate-limiting:
      limit_strategy: total_tokens
      rejected_code: 429
      rejected_msg: token budget exhausted for qwen3-14b-awq
      show_limit_quota_header: true
      rules:
      - count: ${http_x_fleet_token_quota ?? 1000}
        time_window: 3600
        key: $http_x_fleet_team
        header_prefix: Team
    ai-proxy-multi:
      fallback_strategy:
      - http_429
      - http_5xx
      max_retries: 2
      retry_on_failure_within_ms: 2000
      timeout: 120000
      keepalive: true
      ssl_verify: false
      balancer:
        algorithm: roundrobin
      instances:
      - name: qwen3-14b-awq-node1-p8000
        provider: openai-compatible
        weight: 1
        priority: 0
        auth:
          header:
            Authorization: Bearer fleet-internal-unused
        options:
          model: qwen3-14b-awq
        override:
          endpoint: http://sf-qwen3-14b-awq-node1-r0.spark-fleet.svc.cluster.local:8000/v1/completions
        checks:
          active:
            type: http
            http_path: /health
            timeout: 2
            healthy:
              interval: 2
              successes: 2
            unhealthy:
              interval: 1
              http_failures: 3
              timeouts: 3
              http_statuses:
              - 429
              - 500
              - 502
              - 503
              - 504
      - name: qwen3-14b-awq-node3-p8000
        provider: openai-compatible
        weight: 1
        priority: 0
        auth:
          header:
            Authorization: Bearer fleet-internal-unused
        options:
          model: qwen3-14b-awq
        override:
          endpoint: http://sf-qwen3-14b-awq-node3-r0-serve-svc.spark-fleet.svc.cluster.local:8000/v1/completions
        checks:
          active:
            type: http
            http_path: /health
            timeout: 2
            healthy:
              interval: 2
              successes: 2
            unhealthy:
              interval: 1
              http_failures: 3
              timeouts: 3
              http_statuses:
              - 429
              - 500
              - 502
              - 503
              - 504
    prometheus:
      prefer_name: true
- id: sf-chat-unknown-model
  name: sf-chat-unknown-model
  uri: /v1/chat/completions
  methods:
  - POST
  priority: 1
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local model = "unknown"
          local body = core.request.get_body()
          if body then
            local data = core.json.decode(body)
            if data and type(data.model) == "string" then
              model = data.model
            end
          end
          return 404, {
            object = "error",
            type = "NotFoundError",
            code = 404,
            message = "The model `" .. model
              .. "` is not in the fleet catalog (or the request is not application/json).",
          }
        end
    prometheus:
      prefer_name: true
- id: sf-completion-unknown-model
  name: sf-completion-unknown-model
  uri: /v1/completions
  methods:
  - POST
  priority: 1
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local model = "unknown"
          local body = core.request.get_body()
          if body then
            local data = core.json.decode(body)
            if data and type(data.model) == "string" then
              model = data.model
            end
          end
          return 404, {
            object = "error",
            type = "NotFoundError",
            code = 404,
            message = "The model `" .. model
              .. "` is not in the fleet catalog (or the request is not application/json).",
          }
        end
    prometheus:
      prefer_name: true
- id: sf-embedding-unknown-model
  name: sf-embedding-unknown-model
  uri: /v1/embeddings
  methods:
  - POST
  priority: 1
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local model = "unknown"
          local body = core.request.get_body()
          if body then
            local data = core.json.decode(body)
            if data and type(data.model) == "string" then
              model = data.model
            end
          end
          return 404, {
            object = "error",
            type = "NotFoundError",
            code = 404,
            message = "The model `" .. model
              .. "` is not in the fleet catalog (or the request is not application/json).",
          }
        end
    prometheus:
      prefer_name: true
- id: sf-models
  name: sf-models
  uri: /v1/models
  methods:
  - GET
  plugins:
    openid-connect:
      bearer_only: true
      use_jwks: true
      discovery: https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration
      client_id: API_CLIENT_ID
      ssl_verify: true
      token_signing_alg_values_expected: RS256
      accept_none_alg: false
      accept_unsupported_alg: false
      realm: spark-fleet
      claim_validator:
        issuer:
          valid_issuers:
          - https://login.microsoftonline.com/TENANT_ID/v2.0
        audience:
          claim: aud
          required: true
          match_with_client_id: true
      set_userinfo_header: true
      set_access_token_header: false
      set_id_token_header: false
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          local core = require("apisix.core")
          local ui_b64 = core.request.header(ctx, "X-Userinfo")
          if not ui_b64 then
            return 401, { error = "no validated token context" }
          end
          local ui_json = ngx.decode_base64(ui_b64)
          local claims = ui_json and core.json.decode(ui_json) or nil
          if not claims then
            return 401, { error = "cannot decode userinfo" }
          end
          local have = {}
          if type(claims.roles) == "table" then
            for _, r in ipairs(claims.roles) do
              have[r] = true
            end
          end
          local team
          if not team and have["Fleet.TeamAlpha"] then team = "team-alpha" end
          if not team and have["Fleet.TeamQA"] then team = "team-qa" end
          if not team then
            return 403, { error = "token carries no fleet role" }
          end
          local catalog = {
            ["team-alpha"] = { "bge-m3", "qwen3-14b-awq" },
            ["team-qa"] = { "qwen3-14b-awq" },
          }
          local data = {}
          for _, name in ipairs(catalog[team] or {}) do
            data[#data + 1] = { id = name, object = "model", owned_by = "spark-fleet" }
          end
          return 200, { object = "list", data = data }
        end
    prometheus:
      prefer_name: true
- id: sf-healthz
  name: sf-healthz
  uri: /healthz
  methods:
  - GET
  plugins:
    serverless-pre-function:
      phase: access
      functions:
      - |-
        return function(conf, ctx)
          return 200, { status = "ok", service = "spark-fleet-gateway" }
        end
#END
